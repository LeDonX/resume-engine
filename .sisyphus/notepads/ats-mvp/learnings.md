# Learnings

- 2026-03-05: `basicInfo` path is fully wired from schema -> form -> preview -> persistence (`normalizeResumeData`, `renderBasicInfoForm`, `renderBasicInfo`, `saveDraft`).
- 2026-03-05: Handle-only sorting is implemented through `Sortable.create(... handle: ".basic-info-drag-handle")` with button fallback for keyboard-like operation.
- 2026-03-05: Preview hide-empty behavior works in practice (single empty hides one item; all empty hides full basic-info block).
- 2026-03-05: Print button flow correctly triggers pre-print rerender (`rerenderResumeNow`) before `window.print()`.
- 2026-03-05: Pagination stress with 40-line experience bullets yields 3 pages and preserves tail content (`压力条目-40`).
- 2026-03-05: Task2 gap fixed by hard-capping normalized `basicInfo` to 5 items via `normalized.slice(0, 5)` in `normalizeBasicInfoList`.
- 2026-03-05: Importing a draft containing 7 basicInfo entries now renders exactly 5 cards and 5 preview rows.
- 2026-03-05: Task3 verified - icon picker implementation includes: 7 preset options (phone/email/salary/birth/education/location/website), visual grid picker with click-to-select, preset/custom mode toggle, custom Font Awesome input, and fallback to preset when customIcon is empty. Browser tests confirmed: preset selection updates preview immediately, custom mode accepts FA classes (e.g., "fas fa-gem"), empty custom falls back gracefully to preset icon without blank.
- 2026-03-05: Task4 verified - sorting capability: Sortable is handle-only (`handle: ".basic-info-drag-handle"` at 简历.html:1302), up/down buttons work correctly via `moveItem` + `renderAll()` + `saveDraft()`. Button test: clicked "下移" on 手机 (first item) -> order changed from 手机/邮箱/期望薪资/出生年月/学历 to 邮箱/手机/期望薪资/出生年月/学历 immediately in preview. Persistence verified: after page reload, message showed "已恢复上次草稿" and order remained 邮箱/手机/期望薪资/出生年月/学历 (localStorage draft correctly saved).
12: - 2026-03-05: Task5 verified - empty value auto-hide + icon container consistency:
    - A1: Cleared email field (first item) → preview reduced from 5 to 4 items (phone/salary/birth/education visible, email hidden). Implementation: `renderBasicInfo` filters with `.filter((item) => pickText(item?.value, "").trim() !== "")`.
    - A2: Cleared all 5 fields → entire "基本信息" section disappeared from left column preview. Implementation: `buildLeftColumnBlocks` only adds basicInfo block when `renderBasicInfo()` returns truthy content; returns "" when all empty.
    - A3: Verified icon container class = "w-8 h-8 rounded-full bg-blue-100 text-theme flex items-center justify-center mr-3" via Playwright DOM evaluation.
- 2026-03-06: R2 implemented - resolveBasicInfoIcon invalid custom class fallback:
    - Added isValidFontAwesomeClass() helper with "balance rule" validation
    - Balance rule: must have at least one style token (fa-solid/fa-regular/fa-light/fa-thin/fa-brands OR legacy fas/far/fal/fat/fab/fad)
    - AND must have at least one fa-<name> icon token (excluding style tokens like fa-solid and helpers like fa-spin)
    - Invalid cases return false: empty string, style-only (fa-solid), helper-only (fa-spin), no prefix (star)
    - resolveBasicInfoIcon now validates customIcon - invalid or empty falls back to preset
    - Valid custom icons: fas fa-star, fa-solid fa-phone, far fa-envelope, fas fa-star fa-spin
    - Maintains backward compatibility with legacy format (fas/far/fal/fat/fab/fad)
    - Uses existing preset fallback path, no new branch states
    - LSP diagnostics clean, syntax validated
- 2026-03-06: Task6 verified - event bus and persistence consistency:
    - A1: Input changes persist - modified 手机 value → saved to localStorage immediately → reload restores exact value.
    - A2: Icon mode switch persists - switched from preset to custom mode → saved to localStorage → reload shows "自定义图标" label.
    - A3: Custom icon persists - set customIcon to "fas fa-gem" → saved → reload shows diamond icon () in preview.
    - A4: Sorting persists - moved 手机 down (邮箱→手机) → saved → reload shows same order.
    - A5: Import error handling - imported invalid JSON → error message shown → page does NOT crash.
    - Implementation: `applyFieldUpdate` handles basicInfo via section="basicInfo", all `handleAction` cases call `renderAll()` + `saveDraft()`, picker closes via `activeBasicInfoPickerIndex = -1`.
    - A6: Custom icon invalid fallback - set customIcon to "invalid-class" (iconMode=custom) → preview shows preset icon instead of broken icon
    - Implementation: isValidFontAwesomeClass() returns false → falls back to getBasicInfoIconByPreset()
    - A7: Custom icon empty fallback - set customIcon to "" (iconMode=custom) → preview shows preset icon
    - Implementation: existing empty check combined with new validation
    - A8: Valid custom icon works - set customIcon to "fas fa-gem" → preview shows diamond icon (rendered correctly)
- 2026-03-06: Task7 verified - pagination and print regression:
    - A1 (multi-page visible): Page count = 2, split hint present = "分页分割区域 · 第 1/2 页"
    - A2 (print pre-render): Print button handler at 简历.html:1517-1522 calls `rerenderResumeNow()` then `window.print()` via requestAnimationFrame
    - A3 (no runtime errors): Console shows 0 errors (only non-blocking Tailwind CDN warning)
    - No code changes needed - basicInfo updates did not introduce any pagination/print regressions
- 2026-03-06: R1 implemented - normalizeBasicInfoList whitelist convergence:
    - Added VALID_BASIC_INFO_IDS whitelist Set from BASIC_INFO_PRESETS ids (phone/email/salary/birth/education)
    - Filter step: removes unknown ids, only keeps items matching preset ids
    - Deduplication step: uses Set to track seen ids, keeps first occurrence only
    - User order preserved: valid items maintain their import/loadDraft order
    - Fill missing: appends missing presets in BASIC_INFO_PRESETS order
    - Hard constraint: always returns exactly 5 items via slice(0, 5)
    - Icon fields (iconPreset/iconMode/customIcon) preserved with existing fallback logic
    - LSP diagnostics clean, no syntax errors
