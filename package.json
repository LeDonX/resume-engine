{
  "name": "resume-engine",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Vite vanilla resume generator",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "preview:cloudflare": "npm run build && wrangler pages dev dist-static",
    "deploy:cloudflare": "npm run build && wrangler pages deploy dist-static"
  },
  "dependencies": {
    "@fontsource/noto-sans-sc": "^5.2.6",
    "@fortawesome/fontawesome-free": "^6.7.2",
    "sortablejs": "^1.15.6"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "postcss": "^8.5.3",
    "tailwindcss": "^3.4.17",
    "vite": "^5.4.14",
    "wrangler": "^4.68.0"
  }
}
